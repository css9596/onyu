# Claude Code 첫 세션 프롬프트

> 사용법:
> 1. 프로젝트 폴더 만들기: `mkdir onyu && cd onyu`
> 2. 위에서 받은 `CLAUDE.md` 파일을 이 폴더 루트에 복사
> 3. Claude Code 실행: `claude`
> 4. 아래 프롬프트를 그대로 복사해서 붙여넣기

---

## 첫 번째 프롬프트 (이걸 그대로 붙여넣으세요)

```
안녕. 이 프로젝트는 CLAUDE.md에 전체 컨텍스트가 정리되어 있어.
먼저 CLAUDE.md를 읽고 프로젝트를 이해해줘.

오늘 우리가 할 작업은 "프로젝트 초기 셋업"이야. 구체적으로는:

1. Flutter 프로젝트 생성 (app/ 디렉터리)
2. Supabase 프로젝트 구조 셋업 (supabase/ 디렉터리, supabase CLI 사용)
3. 기본적인 디렉터리 구조 생성 (CLAUDE.md에 명시된 구조)
4. .gitignore, README.md 초안 작성
5. Flutter 앱이 Supabase에 연결되는지 확인하는 최소 코드 작성
   (단순히 Supabase 클라이언트 초기화하고 콘솔에 "connected" 출력)

진행하기 전에 다음 사항을 확인해줘:

A. 내 로컬 환경 점검:
   - Flutter SDK 설치 여부 (flutter --version)
   - Supabase CLI 설치 여부 (supabase --version)
   - Node.js 설치 여부 (node --version)
   둘 중 없는 게 있으면 설치 가이드를 안내해줘.

B. 진행 전에 나에게 결정을 물어봐야 할 것:
   - Flutter 프로젝트의 패키지 이름 (예: com.example.onyu)
   - Supabase는 클라우드 호스팅으로 갈지, 로컬 개발 후 나중에 클라우드 연결할지
   - 앱 지원 최소 버전 (Android API 레벨, iOS 버전)

C. 작업 방식:
   - 한 단계씩 진행하고, 각 단계 끝나면 결과를 보여주고 다음으로 넘어갈지 물어봐줘.
   - 명령어를 실행하기 전에 어떤 명령어를 왜 실행하는지 설명해줘.
   - 파일을 생성/수정하기 전에 무엇을 만들지 먼저 알려줘.

준비됐으면 먼저 환경 점검부터 시작해줘.
```

---

## 이 프롬프트가 왜 이렇게 짜였는지 (이해해두면 다음 프롬프트도 잘 짤 수 있음)

### 1. CLAUDE.md를 먼저 읽으라고 명시
Claude Code는 자동으로 CLAUDE.md를 읽지만, 명시적으로 "읽고 이해했는지 확인"하는 게 안전해요. 이렇게 하면 첫 응답에서 Claude Code가 프로젝트 컨텍스트를 잘 파악했는지 식님이 검증할 수 있어요.

### 2. "오늘 할 작업"을 명확히 한정
큰 비전 말고 오늘의 작업만 적어요. Claude Code가 너무 멀리 나가지 않게 하는 안전장치예요.

### 3. 환경 점검을 먼저 시키기
이게 안 되어 있으면 뒤에 다 막혀요. 먼저 점검하라고 시키는 게 시간 절약이에요.

### 4. "결정 물어봐줘"를 명시
이게 핵심이에요. Claude Code는 시키면 알아서 결정해버리는 경향이 있어요. 비즈니스 결정(패키지 이름, 호스팅 방식 등)은 식님이 해야 하니까 명시적으로 물어보게 만들어요.

### 5. "한 단계씩, 설명하면서"
한 번에 100개 명령어 실행하면 뭐가 어떻게 됐는지 모르고 끝나요. 단계별로 가야 식님이 학습도 되고 통제도 가능해요.

---

## 다음 세션 프롬프트들 (참고용 — 미리 만들어두지는 마세요)

각 단계가 끝나면 새 세션을 열고 다음 작업을 던지는 식으로 진행하세요.
미리 다 만들어두면 이전 단계 결과에 따라 수정해야 할 게 많아져요.

**예상 다음 단계:**

1. ✅ 프로젝트 초기 셋업 (이번 세션)
2. 데이터 모델 설계 및 첫 마이그레이션
   ("CLAUDE.md를 읽고 다음 작업으로 넘어가자. 사주 앱의 데이터 모델을
   설계하고 첫 Supabase 마이그레이션 SQL을 작성해줘. 테이블은
   profiles, saju_profiles, conversations, messages, subscriptions 5개야.
   먼저 ERD를 의사코드로 보여주고 내 검토 받은 후 SQL로 변환해줘.")

3. 사주 계산 라이브러리 통합
   ("한국어 만세력 라이브러리 후보를 3개 정도 찾아서 비교 분석 표를
   만들어줘. 라이선스, 정확도, 유지보수 활성도, 사용 편의성 기준으로.
   pub.dev에서 dart 라이브러리도 찾고, 없으면 알고리즘 직접 구현하는
   방향도 검토해줘.")

4. Edge Function 골격 작성 (Claude API 호출)
   ("supabase/functions/chat/ 안에 Claude API를 호출하는 Edge Function의
   기본 구조를 작성해줘. 인증 검증 → 사용량 한도 체크 → 사주 정보 조회 →
   시스템 프롬프트 조립 → Claude API 호출 → 응답 저장 → 클라이언트 반환.
   각 단계별로 함수를 분리하고, 시스템 프롬프트는 일단 더미로 둬.")

5. 인증 및 온보딩 플로우
6. 대화 화면 UI
7. 대화 화면 ↔ Edge Function 연동
8. 무료/프리미엄 구분 및 일일 한도 로직
9. 인앱결제 통합
10. 시스템 프롬프트 적용 및 실사용자 톤 튜닝
11. 베타 테스트 빌드
12. 스토어 출시 준비

---

## 진행하면서 주의할 점

### 새 세션 시작할 때마다 (Claude Code는 세션 간 메모리 없음)
첫 메시지에 항상 이렇게 시작하세요:
```
CLAUDE.md를 읽고 현재 진행 상황을 확인한 다음 [오늘 할 작업]을 시작하자.
```
이러면 Claude Code가 매번 컨텍스트를 다시 잡아요.

### 막혔을 때
Claude Code가 헛바퀴 돌면 (같은 에러 반복, 이상한 코드 생성 등) 세션 종료하고 새로 시작하세요. 컨텍스트가 오염된 거예요.

### Git 커밋
한 단계 끝날 때마다 git commit 하는 습관을 들이세요. Claude Code한테
"git status 보여주고 의미 있는 단위로 커밋해줘"라고 시키면 돼요.

### CLAUDE.md 업데이트
중요한 결정이 바뀌면 CLAUDE.md도 같이 업데이트해야 해요.
"방금 결정한 내용을 CLAUDE.md에 반영해줘"라고 시키세요.
